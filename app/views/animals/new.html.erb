<!-- This is the page to add a new animal -->
<h2 class= "pg-title">Add a new Animal</h2>
<style>
.select-item{
  border:solid;
  border-width: 1px;
  padding:3px;
  border-radius:5px;

  margin:4px;

}
.breed-item{
  border:solid;
  border-width: 1px;
  padding:3px;
  border-radius:5px;

  margin:4px;
  width: 100%;
}
.remove-item{
  position: absolute;
  top: 0;
  right: 0;
  cursor: pointer;
}
</style>
<div class= "nav-form-container col-md-12">

  <!-- Nav Bar Content -->
  
  <div class="form-nav col-md-2">
    <!-- Personal info tab -->
    <div class="card form-div personal col-md-12">
      <a class="card-header personal-header" data-toggle="collapse" href="#personal-body">
        <div style="background: white;">
          <h4>Characteristics</h4>
        </div>
      </a>
    </div>

    <div class= "card form-div personal col-md-12">
      <a class= "card-header paper-header" data-toggle="collapse" href="#paper-body">
        <div>
          <h4>Paperwork</h4>
        </div>
      </a>
    </div>

    <div class="form-div tracking col-md-12">
      <a class= "card-header tracking-header" data-toggle="collapse" href="#tracking-body">
        <div>
          <h4>Tracking</h4>
        </div>
      </a>
    </div>

    <div class="form-div alerts col-md-12">
      <a class= "card-header alert-header" data-toggle="collapse" href="#alerts-body">
        <div>
         <h4>Alerts</h4>
        </div>
      </a>
    </div>

  </div>

  <!-- Inner form Content -->

  <div class="form col-md-10">
    <%= form_for(@animal, url: animals_path(@animal))  do |f| %>
      
      <!-- Personal Inner Div -->
      <div id ="personal-body" class="collapse show inner-formdiv">
        
        <div class="bsc-chracteristics col-md-6">
          <!--Basic Characteristics Header -->
          <a class= "card-header pers-header" data-toggle="collapse show" href="#characteristics-body">
            <div>
              <h4> Basic Characteristics</h4>
            </div>
          </a>

          <!--Basic Characteristics Body -->
          <div id = "characteristics-body" class="collapse show col-md-12">
            <!-- Profile Picture -->
            <div class="picture col-md-6">
              <%= f.label :picture %>
              <div id="pic-preview" style="padding: 5px">
                <span class="glyphicon glyphicon-camera" aria-hidden="true"></span>
                <!-- alt="Dog Image"-->
              </div>

              <img id="profilepic" src="#" alt="Dog Image" style="display: none"/>
              <%= f.file_field :picture, accept: 'image/jpeg,image/gif,image/png', class: 'file-upload profile-file' %>
            </div>

            <!-- Basic Characteristics -->
            <div class="characteristics-info col-md-6">
              <%= f.label :name %>
              <%= f.text_field :name, class: 'form-control required' %>

              <%= f.label :primary_breed_id, "Primary Breed" %>
              <%= f.collection_select :primary_breed_id, @breed, :id, :name, {:prompt => "Select Breed"}, class:'required' %>

               <%= label :post, "Additional Breeds" %>
              <%= collection_select(:breed_id, 0, @breed, :id, :name, {:prompt => "Select Breed"}, {class: "breed-selecter"}) %>              
              <div id = "breeds-list"></div>

              <%= text_field_tag 'breeds', "", class: "hidden-breeds", style: "display:none;"  %>
             

              <%= f.label :date_of_birth %>
              <%= f.text_field :birthday, class: 'datepicker required' %>

              <%= f.label :microchip_number , "Microchip #"%>
              <%= f.text_field :microchip_number, class: 'form-control' %>

              <%= f.label :status %>
              <%= f.collection_select :status_id, @status, :id, :name, {:prompt => "Select Status"}, class: 'required' %>
            </div>
          </div>
        </div>

        <div class = "phy-med-frame col-md-6">
          <div class = "phys-features col-md-12">
            <!-- Physical Features -->
            <a class= "card-header doc-header" data-toggle="collapse" href="#phys-feat">
              <div>
                <h4>Physical Features</h4>
              </div>
            </a>

            <div id="phys-feat" class="collapse col-md-12">
              <%= f.label :gender %>
              <%= f.select :gender, [ 'Male','Female', 'Unknown'], :prompt => 'Select Gender' %>    

              <%= f.label :color_primary %>
              <%= f.text_field :color_primary, class: 'form-control' %>

              <%= f.label :color_secondary %>
              <%= f.text_field :color_secondary, class: 'form-control' %>             
            </div>
          </div>


          <div class = "med col-md-12">
            <!-- Personality -->
            <a class= "card-header doc-header" data-toggle="collapse" href="#pers-body">
              <div>
                <h4>Personality</h4>
              </div>
            </a>

            <div id="pers-body" class="collapse col-md-12">
              <%= label :post, "behavior" %>
              <%= collection_select(:behavior_id, 0, @behavior, :id, :name, {:prompt => "Select behavior"}, {class: "behavior-selecter"}) %>              
              <div id = "behavior-list"></div>

              <%= text_field_tag 'behavior', "", class: "hidden-behavior", style: "display:none;"  %>

              <%= label :post, "attribute" %>
              <%= collection_select(:attribute_id, 0, @attribute, :id, :name, {:prompt => "Select attribute"}, {class: "attribute-selecter"}) %>              
              <div id = "attribute-list"></div>

              <%= text_field_tag 'attribute', "", class: "hidden-attribute", style: "display:none;"  %>
            </div>
          </div>
        </div>
      </div>


      <!-- Paperwork -->
      <div id="paper-body" class="collapse inner-formdiv">
        <div id="upload-buttons" style="padding-top: 15px">

        <div class = "upload-wrapper" style= "padding-bottom: 15px">
            <span class="btn btn-large btn-warning btn-file">
                <%= label_tag "Vetting Document" %>
                <%= f.file_field :vetting_document, class: 'file-upload' %>
            </span>
            <span class = "file-name"></span>
            <span class = "glyphicon glyphicon-remove remove-file" style = "display:none"></span>
        </div>

        <div class = "upload-wrapper" style= "padding-bottom: 15px">
            <span class="btn btn-large btn-primary btn-file">
                <%= label_tag "Intake Document" %>
                <%= f.file_field :intake_document, class: 'file-upload' %>
            </span>
            <span class = "file-name"></span>
            <span class = "glyphicon glyphicon-remove remove-file" style = "display:none"></span>
         </div>

         <div class = "upload-wrapper" style= "padding-bottom: 15px">
            <span class="btn btn-large btn-info btn-file">
                <%= label_tag "Owner Surrender" %>
                <%= f.file_field  :owner_surrender_document, class: 'file-upload' %>
            </span>
            <span class = "file-name"></span>
            <span class = "glyphicon glyphicon-remove remove-file" style = "display:none"></span>
        </div>

        <div class = "upload-wrapper" style= "padding-bottom: 15px">
          <span class="btn btn-large btn-info btn-file">
              <%= label_tag "Home Check" %>
              <%= f.file_field  :home_check_document, class: 'file-upload' %>
          </span>
          <span class = "file-name"></span>
          <span class = "glyphicon glyphicon-remove remove-file" style = "display:none"></span>
        </div>

        <div class = "upload-wrapper" style= "padding-bottom: 15px">
          <span class="btn btn-large btn-info btn-file">
              <%= label_tag "Match Application" %>
              <%= f.file_field  :match_application_document, class: 'file-upload' %>
          </span>
          <span class = "file-name"></span>
          <span class = "glyphicon glyphicon-remove remove-file" style = "display:none"></span>
        </div>

        <div class = "upload-wrapper" style= "padding-bottom: 15px">
          <span class="btn btn-large btn-info btn-file">
              <%= label_tag "Adoption Application" %>
              <%= f.file_field  :adoption_application_document, class: 'file-upload' %>
          </span>
          <span class = "file-name"></span>
          <span class = "glyphicon glyphicon-remove remove-file" style = "display:none"></span>
        </div>

        <div class = "upload-wrapper">
          <span class="btn btn-large btn-info btn-file">
              <%= label_tag "Adoption Contract" %>
              <%= f.file_field  :adoption_contract_document, class: 'file-upload' %>
          </span>
          <span class = "file-name"></span>
          <span class = "glyphicon glyphicon-remove remove-file" style = "display:none"></span>
        </div>
        </div>
      </div>

      <!--========================== TRACKING =============================-->
      <div id="tracking-body" class="collapse inner-formdiv">
        <!--========== BEGIN DROPDOWN OPTIONS =========-->


        <!--========== END DROPDOWN OPTIONS =========-->

        <!--=========== ADD HOLD ==========-->
        <!--<div id="hold-sel" class="peopleFostDiv col-md-4">
          <button type="button" class="btn btn-primary btn-md" data-toggle="modal" data-target="#holdModal">
            Add Hold
          </button>-->

          <!-- Modal -->
          <!--<div class="modal fade" id="holdModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
            <div class="modal-dialog" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                  <h4 class="modal-title" id="myModalLabel">Add Hold</h4>
                </div>
                
                <div class="modal-body">-->
                  
                  <!-- Hold option selections -->
                  <!--<h3>Select Hold Type</h3>
                  <div class="hold=options">
                    <button id="illness_btn" type="button" class="btn btn-primary btn-md col-md-3">Illness</button>
                    <button id="training-btn" type="button" class="btn btn-primary btn-md col-md-3">Training</button>
                    <button type="button" class="btn btn-primary btn-md col-md-3">Other</button>
                  </div>-->

                  <!-- Hold option selected info -->
                  <!--<div id="illness-info" class="col-md-12">
                    <p> Show if ilness button clicked </p>
                  </div>
                  <div id="training-info" class="col-md-12">
                    <p> Show if training button clicked </p>
                  </div>
                  <div id="other-info" class="col-md-12">
                    <p> Show if other button clicked </p>
                  </div>
                </div>

                <div class="modal-footer">
                  <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                  <button type="button" class="btn btn-primary">Add Hold</button>
                </div>
                
              </div>
            </div>
          </div>            
        </div>-->
        <!--=========== END ADD HOLD ==========-->       


    
        <!--========== BEGIN TRACKING TABS =========-->
        <div id= "trackingInner-div" class="col-md-12">
          
          <div id="status-tabs" class="trackingInner-div1 col-md-8">
            <!-- Tab headers for each status -->
            <ul class="nav nav-tabs" role="tablist">
              <li role="presentation"><a href="#intake" data-toggle="tab">Intake</a></li>
              <li role="presentation"><a href="#vetting" data-toggle="tab">Vetting</a></li>
              <li role="presentation"><a href="#foster" data-toggle="tab">Foster</a></li>
              <li role="presentation"><a href="#adopted" data-toggle="tab">Adopted</a></li>
              <li role="presentation"><a href="#sleep" data-toggle="tab">Sleep</a></li>
              <li role="presentation"><a href="#transfer" data-toggle="tab">Transfer</a></li>
            </ul>

            <!-- Information for Statuses in Tabs-->
            <div class="status-tabsInner tab-content">
              <!-- INTAKE -->
              <div id = "intake" role="tabpanel" class="tab-pane fade in">
                <!-- Selecting Date-->
                <%= f.label :intake_date, "Intake Date" %>
                <%= text_field_tag 'intake_dt', "" ,class: 'datepicker' %>
                
                <!-- Selecting Foster-->
                <%= f.label :intake_fost, "Foster"%>
                <%= collection_select(:intake_fost, :user_id, User.all, :id, :name, {:prompt => "Select foster"}) %>
                
                <!-- Selecting Vets-->
                <%= f.label :intake_vet, "Veterinarian"%>
                <%= collection_select(:intake_vet, :veterinarian_id, Veterinarian.all, :id, :name, {:prompt => "Select Current Veterinarian"}) %>

                <!-- Inputing Details-->
                <%= f.label :intake_comm, "Details" %>
                <%= text_field_tag 'intake_cm', "", class: 'form-control' %>

                <%#= f.label :intake_hold, "Hold"%>
                <%#= collection_select(:intake_hold, :hold_type_id, HoldType.all, :id, :name, {:prompt => "Select a Hold"}) %>
              
              </div> 


              <!-- VETTING -->
              <div id="vetting" role="tabpanel" class="tab-pane">
                  <%= f.label :vetting_date %>
                  <%= text_field_tag 'vet_dt', "" ,class: 'datepicker' %>
                

                  <!-- Selecting Foster-->
                  <%= f.label :vet_fost, "Foster"%>
                  <%= collection_select(:vet_fost, :user_id, User.all, :id, :name, {:prompt => "Select foster"}) %>
                  
                  <!-- Selecting Vets-->
                  <%= f.label :vet_vet, "Veterinarian"%>
                  <%= collection_select(:vet_vet, :veterinarian_id, Veterinarian.all, :id, :name, {:prompt => "Select Current Veterinarian"}) %>
                  
                  <!-- Selecting Holds-->
                  <%#= f.label :vet_hold, "Hold"%>
                  <%#= collection_select(:vet_hold, :hold_type_id, HoldType.all, :id, :name, {:prompt => "Select a Hold"}) %>
                  <!-- Inputing Details-->
                  <%= f.label :vet_comm, "Details" %>
                  <%= text_field_tag 'vet_cm', "", class: 'form-control' %>

              </div>
              
              <!-- FOSTER -->
              <div id="foster" role="tabpanel" class="tab-pane">
                <%= f.label :foster_date %>
                <%= text_field_tag 'foster_dt', "" ,class: 'datepicker' %>
                
                <%= f.label :fost_fost, "Foster"%>
                <%= collection_select(:fost_fost, :user_id, User.all, :id, :name, {:prompt => "Select foster"}) %>

                <!-- Selecting Holds-->
                <%#= f.label :fost_hold, "Hold"%>
                <%#= collection_select(:fost_hold, :hold_type_id, HoldType.all, :id, :name, {:prompt => "Select a Hold"}) %>

                <%= f.label :fost_comm, "Details" %>
                <%= text_field_tag 'fost_cm', "", class: 'form-control' %>
              </div>
              
              <!-- ADOPTED -->
              <div id="adopted" role="tabpanel" class="tab-pane">
                <%= f.label :adopt_date %>
                <%= text_field_tag 'adopted_dt', "", class: 'form-control' %>
                
                <%= f.label :adopt_comm, "Details" %>
                <%= text_field_tag 'adopted_cm', "", class: 'form-control' %>              
              </div>
              
              <!-- SLEEP -->
              <div id="sleep" role="tabpanel" class="tab-pane">
                <%= f.label :sleep_date %>
                <%= text_field_tag 'sleep_dt', "", class: 'form-control' %>
                
                <%= f.label :sleep_comm, "Details" %>
                <%= text_field_tag 'sleep_cm', "", class: 'form-control' %>
              </div>
              
              <!-- TRANSFER -->
              <div id="transfer" role="tabpanel" class="tab-pane">
                <%= f.label :transfer_date %>
                <%= text_field_tag 'transfer_dt', "", class: 'form-control' %>
                
                <%= f.label :transfer_comm, "Details" %>
                <%= text_field_tag 'transfer_cm', "", class: 'form-control' %> 
              </div>
            </div>
          </div>
          <!--========== END TRACKING TABS =========-->


          <!--===================== BEGIN ADD PEOPLE ========================-->
          <!-- <div id="foster sel" class="trackingInner-div2 col-md-4"> -->
            

            <!-- BEGIN ADD FOSTER -->
            <!--<div id="foster sel" class="peopleFostDiv col-md-12">
              <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#fostModal">
                Add Foster
              </button> -->

              <!-- Modal -->
              <!--<div class="modal fade" id="fostModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                <div class="modal-dialog" role="document">
                  <div class="modal-content">
                    <div class="modal-header">
                      <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                      <h4 class="modal-title" id="myModalLabel">Add Foster</h4>
                    </div>
                    <div class="modal-body">
                      ...
                    </div>
                    <div class="modal-footer">
                      <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                      <button type="button" class="btn btn-primary">Add Foster</button>
                    </div>
                  </div>
                </div>
              </div>            
            </div> -->
            <!-- END ADD FOSTER -->

            <!-- BEGIN ADD VET -->
            <!-- <div id="vet sel" class="peopleVetDiv col-md-12"> -->
              <!-- Button trigger modal -->
              <!--<button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#vetModal">
                Add Veterenarian
              </button> -->

              <!-- Modal -->
              <!--<div class="modal fade" id="vetModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                <div class="modal-dialog" role="document">
                  <div class="modal-content">
                    <div class="modal-header">
                      <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                      <h4 class="modal-title" id="myModalLabel">Add Vet</h4>
                    </div>
                    <div class="modal-body">
                      ...
                    </div>
                    <div class="modal-footer">
                      <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                      <button type="button" class="btn btn-primary">Add Vet</button>
                    </div>
                  </div>
                </div>
              </div>            
            </div> -->
            <!-- END ADD VET -->
          <!--</div> -->
          <!--===================== END ADD PEOPLE ========================-->

        </div>  
      </div>

      <!-- Alerts -->
      <div id="alerts-body" class="collapse inner-formdiv">
          <p> Functions for Alerts</p>
      </div>
    
      <!-- Can i move this? -->
      <%= f.submit "Add animal", :onclick => "return validateform();", class: "add-btn btn-primary" %>
    <% end %>
  </div>
</div>




</div>
<script>
  // //Tracking show modals
  // function showDiv() {
  //  document.getElementById('welcomeDiv').style.display = "block";
  // }
  // $("")







  $("#animal_species_id").change(function(){
    console.log($("#animal_breed_id"))
    if ($("#animal_species_id").val() == 1) {
      $("#animal_breed_id option:eq(1)").hide();
      $("#animal_breed_id option:eq(0)").show();
    } else {
      $("#animal_breed_id option:eq(0)").hide();
      $("#animal_breed_id option:eq(1)").show();

    }
    console.log("Hey I changed :)")
  });


  $('.datepicker').datepicker({
    endDate: '-0d'
  });

  $(".profile-file").change(function(input) { 
    input = input.target
    if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                $('#profilepic')
                    .attr('src', e.target.result)
                    .width(150)
                    .height(200);
                $('#pic-preview').hide()  
                $('#profilepic').show()
            };

            reader.readAsDataURL(input.files[0]);

        }
  });

  $(".file-upload").change(function(input) { 
    var input = input.target;
    if (input.files && input.files[0]) {
        var fileName = input.files[0].name;
        var container =  $(this).closest(".upload-wrapper");
        var nameSpan = container.find(".file-name");
        nameSpan.text(fileName);
        var removeSpan = container.find(".remove-file");
        removeSpan.show();
        }
  }); 

  $(".remove-file").on('click', function(){
        var container =  $(this).closest(".upload-wrapper");

        nameSpan = container.find(".file-name");
        nameSpan.text("");

        var removeSpan = container.find(".remove-file");
        removeSpan.hide();

        var inputfield  = container.find(".file-upload");
        inputfield[0].value = "";

  });
  $(".breed-selecter").on("change", function(){
    var text = $(".breed-selecter option:selected").text();
    var val = $(".breed-selecter option:selected").val();
    if(text == "Select Breed"){
      return
    }
    html = $("#breeds-list").html();
    html += "<div id = 'breed-" + val + "'' class = 'breed-item select-item' style = 'position: relative; display: inline-block;'>" 
        + "<p style = 'margin:0px; padding-right:15px'>" + text + "</p>" + 
       "<span class = 'glyphicon glyphicon-remove remove-breed remove-item' style='margin:2px; float:right;'>" +"</span>" + 
       " </div>"
    $("#breeds-list").html(html);
    $(".remove-breed").on("click", function(){
      $(this).closest(".breed-item").detach()
      setHiddenBreeds();
    })
    //just for now while im setting up the new breeds
    $(this).get(0).selectedIndex = 0;
    setHiddenBreeds();
  })
  function setHiddenBreeds(){
    str = ""
    $(".breed-item").each(function(){
      var id = this.id.split("-")[1]
      str += id + "|"
    })
    $(".hidden-breeds").val(str)
  }

  $(".attribute-selecter").on("change", function(){
    var text = $(".attribute-selecter option:selected").text();
    var val = $(".attribute-selecter option:selected").val();
    if(text == "Select Attribute"){
      return
    }
    html = $("#attribute-list").html();
    html += "<div id = 'attribute-" + val + "'' class = 'attribute-item select-item' style = 'position: relative; display: inline-block;'>" 
        + "<p style = 'margin:0px; padding-right:15px'>" + text + "</p>" + 
       "<span class = 'glyphicon glyphicon-remove remove-attribute remove-item' style='margin:2px; float:right;'>" +"</span>" + 
       " </div>"
    $("#attribute-list").html(html);
    $(".remove-attribute").on("click", function(){
      $(this).closest(".attribute-item").detach()
      setHiddenAttribute();
    })
    //just for now while im setting up the new breeds
    $(this).get(0).selectedIndex = 0;
    setHiddenAttribute();
  })
  function setHiddenAttribute(){
    str = ""
    $(".attribute-item").each(function(){
      var id = this.id.split("-")[1]
      str += id + "|"
    })
    $(".hidden-attribute").val(str)
  }


  $(".behavior-selecter").on("change", function(){
    var text = $(".behavior-selecter option:selected").text();
    var val = $(".behavior-selecter option:selected").val();
    if(text == "Select Behavior"){
      return
    }
    html = $("#behavior-list").html();
    html += "<div id = 'behavior-" + val + "'' class = 'behavior-item select-item' style = 'position: relative; display: inline-block;'>" 
        + "<p style = 'margin:0px; padding-right:15px'>" + text + "</p>" + 
       "<span class = 'glyphicon glyphicon-remove remove-behavior remove-item' style='margin:2px; float:right;'>" +"</span>" + 
       " </div>"
    $("#behavior-list").html(html);
    $(".remove-behavior").on("click", function(){
      $(this).closest(".behavior-item").detach()
      setHiddenBehavior();
    })
    //just for now while im setting up the new breeds
    $(this).get(0).selectedIndex = 0;
    setHiddenBehavior();
  })
  function setHiddenBehavior(){
    str = ""
    $(".behavior-item").each(function(){
      var id = this.id.split("-")[1]
      str += id + "|"
    })
    $(".hidden-behavior").val(str)
  }


</script>


